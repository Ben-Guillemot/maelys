import React from 'react';
import './footer.scss';

function Footer() {
  return (
    <div className="footer">
      <p>footer</p>
    </div>
  );
}

Footer.propTypes = {
};
Footer.defaultProps = {
};
export default React.memo(Footer);
